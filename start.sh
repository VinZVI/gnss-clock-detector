#!/usr/bin/env bash

# Прерываем выполнение при ошибке
set -e

echo "==========================================="
echo "🚀 Запуск ETL с FTP после деплоя (за 5 дней)"
echo "==========================================="

# Выполняем загрузку данных с FTP за последние 5 дней
python -m gnss_clock.etl --source ftp --days 5

echo "==========================================="
echo "🌐 Запуск веб-сервера Gunicorn"
echo "==========================================="
exec gunicorn wsgi:app --bind 0.0.0.0:$PORT
